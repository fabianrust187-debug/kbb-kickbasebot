# Discord Bot Token from Discord Developer Portal
DISCORD_TOKEN=your_discord_bot_token_here

# Application / Client ID from Discord Developer Portal
CLIENT_ID=your_discord_application_client_id_here

# Optional: Guild ID for fast slash-command registration during development
# If empty, commands are registered globally and can take longer to appear.
GUILD_ID=your_discord_server_id_here

# Optional: default accent color for embeds
KBB_EMBED_COLOR=0x00ff66

# Dedicated channel for experimental/new feature output
KBB_TEST_CHANNEL_ID=1522249317656690929

# Experimental Bundesliga live-goal feed (output remains in KBB_TEST_CHANNEL_ID)
# Poll interval in milliseconds. Minimum enforced by the bot: 20000.
KBB_GOAL_FEED_INTERVAL_MS=30000
# HTTP timeout for ESPN/Kickbase live requests.
KBB_GOAL_FEED_TIMEOUT_MS=10000
# If the bot starts while a match is already live, post at most this many recent goals per match.
KBB_GOAL_INITIAL_BACKFILL=1

# Temporary Champions League goal-feed parser test.
# Match discovery is date + team based; do NOT use widget/game IDs as ESPN IDs.
KBB_GOAL_TEST_LEAGUE=uefa.champions
KBB_GOAL_TEST_DATE=20260909
KBB_GOAL_TEST_HOME=Liverpool
KBB_GOAL_TEST_AWAY=Atletico Madrid
KBB_GOAL_TEST_MATCH_LABEL=Liverpool - Atletico Madrid
# Optional: only set this if the real ESPN event ID is known.
KBB_GOAL_TEST_ESPN_EVENT_ID=
# On first observation of a live match, backfill at most this many recent goals.
KBB_UCL_TEST_INITIAL_BACKFILL=1
# For a completed test match, allow historical goal backfill for parser verification.
KBB_UCL_TEST_COMPLETED_BACKFILL=10

# Production transfer-feed channel
KBB_TRANSFER_CHANNEL_ID=1522249401735839784
# Poll interval in milliseconds. Minimum enforced by the bot: 30000.
KBB_TRANSFER_FEED_INTERVAL_MS=60000
# Number of newest transfers posted on the first production start if no previous feed markers exist.
KBB_TRANSFER_INITIAL_BACKFILL=5

# Top-5 configuration
TOP5_CHANNEL_ID=your_top5_channel_id_here
TOP5_MANAGER_TARGET=14

# Kickbase read-only data integration
# Competition 1 is Bundesliga.
KICKBASE_COMPETITION_ID=1
KICKBASE_LEAGUE_NAME=187 KICKBASEBANDE

# Optional but recommended if known; avoids resolving the league by name.
KICKBASE_LEAGUE_ID=

# Authentication option A: pre-obtained Bearer token.
# Never commit a real token to GitHub; set it only in your hosting environment.
KICKBASE_TOKEN=

# Authentication option B: automatic login/refresh.
# If these are set, the bot can refresh an expired token automatically.
# Store them only in your hosting provider's secret/environment variables.
KICKBASE_EMAIL=
KICKBASE_PASSWORD=

# Optional API timeout in milliseconds
KICKBASE_API_TIMEOUT_MS=10000
